import { AuthForm } from "@/component/AuthForm";

export default function LoginPage() {
  return (
    <div className="flex min-h-full items-center justify-center overflow-y-auto bg-background px-page py-section">
      <AuthForm />
    </div>
  );
}
